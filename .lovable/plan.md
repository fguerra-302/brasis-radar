# Documentação do schema (somente texto)

O pedido é apenas informativo: listar tabelas, colunas e exemplo de registro para documentar o schema. Nenhuma alteração de código ou banco é necessária.

## Entrega
- A resposta completa já foi fornecida no chat: as 15 tabelas do `public` schema, colunas principais, tipos, defaults e um exemplo de linha em JSON para cada uma.
- Notas de integração incluídas: RLS por `user_id`, `shared_sources` global com escrita admin-only, colunas `credentials` sensíveis, e uso da anon key + JWT para leitura externa via PostgREST.

## Ações
- Nenhuma. Se desejar, posso gerar um arquivo `SCHEMA.md` em `/mnt/documents` com esse conteúdo formatado — basta pedir.
